name: release

## Pipeline de distribuição Windows do Verso (app WPF + Blazor Hybrid, net10.0-windows).
## O app é Windows-only (WPF/WebView2 não rodam em Linux), então o runner é windows-latest.
##
## Publica DUAS variantes por release (matrix cpu|gpu), ambas single-file (DLLs gerenciadas
## embutidas no Verso.App.exe/Verso.Worker.exe — raiz limpa, só os dois exes) e portáteis
## (dados em <appdir>/data, não em %AppData%). Verso.Worker.exe (Fase 2/transcricao-cpu-responsiva,
## processo separado onde a transcrição real roda) é publicado no MESMO diretório de output do
## Verso.App, pois WorkerExecutableLocator o resolve como sibling de Verso.App.exe em runtime:
##   - cpu: sem runtimes CUDA/Vulkan (~200 MB) — roda em qualquer máquina
##   - gpu: com runtimes CUDA/Vulkan win-x64 (~960 MB) — aceleração por GPU NVIDIA
## Ambas removem runtimes Linux/macOS e peso morto (pdb/xml/Metal) antes do zip.
##
## Gatilhos:
##   - push para main           -> calcula próxima versão (incrementa patch da última tag),
##                                 builda as 2 variantes, cria a tag, gera GitHub Release
##   - push de tag v*.*.*       -> builda as 2 variantes e cria GitHub Release com os 2 zips
##   - workflow_dispatch        -> builda e publica artifacts (sem Release), útil para validação
on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write   # necessário para criar o GitHub Release e fazer upload dos assets

jobs:
  build:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        variant: [cpu, gpu]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Determinar versão
        id: version
        shell: pwsh
        run: |
          # Tag v*.*.* -> version SEM o 'v' (NuGet rejeita 'v0.1.0');
          # Push para main -> auto-incrementa patch da última tag;
          # Dispatch -> dev version com short SHA.
          if ($env:GITHUB_REF -match 'refs/tags/(v.*)') {
            $tag = $Matches[1]          # v0.1.0
            $ver = $tag.Substring(1)    # 0.1.0
          } elseif ($env:GITHUB_REF -eq 'refs/heads/main') {
            $latest = git tag --list 'v*.*.*' --sort=-v:refname | Select-Object -First 1
            if (-not $latest) {
              $ver = '0.1.0'
            } else {
              $lastVer = $latest.Substring(1)
              $parts = $lastVer.Split('.')
              $patch = [int]$parts[2] + 1
              $ver = "$($parts[0]).$($parts[1]).$patch"
            }
            $tag = "v$ver"
          } else {
            $tag = ''
            $ver = "0.0.0-dev+$($env:GITHUB_SHA.Substring(0,7))"
          }
          Write-Host "tag=$tag version=$ver"
          "tag=$tag" >> $env:GITHUB_OUTPUT
          "version=$ver" >> $env:GITHUB_OUTPUT

      - name: Publicar self-contained (win-x64, single-file)
        shell: pwsh
        run: |
          # Single-file empacota TODAS as DLLs gerenciadas dentro do Verso.App.exe, deixando
          # a raiz limpa (só o app). Nativos (WPF/WebView2/whisper.cpp/CUDA) ficam externos
          # em runtimes/<rid>/native/ — assim ainda dá para remover runtimes CUDA/Vulkan
          # não desejados antes do zip (variante CPU) sem inchar o exe.
          dotnet publish src/Verso.App/Verso.App.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=false `
            -p:Version=${{ steps.version.outputs.version }} `
            -o publish/${{ matrix.variant }}

      - name: Publicar Verso.Worker self-contained (win-x64, single-file)
        shell: pwsh
        run: |
          # Mesmo diretório de output do Verso.App: WorkerExecutableLocator resolve
          # "Verso.Worker.exe" como sibling de Verso.App.exe em runtime (produção, sem
          # o copy target de dev-build de Verso.App.csproj, que só roda em `dotnet build`).
          dotnet publish src/Verso.Worker/Verso.Worker.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=false `
            -p:Version=${{ steps.version.outputs.version }} `
            -o publish/${{ matrix.variant }}

      - name: Remover runtimes e arquivos desnecessários
        shell: pwsh
        run: |
          $pub = "publish/${{ matrix.variant }}"
          # 1. Remove RIDs não-win-x64 (linux/osx/...) dos flavors de GPU (cuda, cuda12, vulkan).
          Get-ChildItem "$pub/runtimes" -Directory -ErrorAction SilentlyContinue |
            Where-Object { $_.Name -match '^(cuda|cuda12|vulkan)$' } |
            ForEach-Object {
              Get-ChildItem $_ -Directory | Where-Object { $_.Name -ne 'win-x64' } | Remove-Item -Recurse -Force
            }
          # 2. Variante CPU: remove CUDA e Vulkan inteiros, deixa só o runtime CPU (runtimes/win-x64).
          if ('${{ matrix.variant }}' -eq 'cpu') {
            Get-ChildItem "$pub/runtimes" -Directory -ErrorAction SilentlyContinue |
              Where-Object { $_.Name -match '^(cuda|cuda12|vulkan)$' } |
              Remove-Item -Recurse -Force
          }
          # 3. Remove runtimes de plataformas estrangeiras (app é win-x64-only): linux, macOS,
          #    ARM/32-bit Windows e coreml (Apple). Mantém win-x64, noavx (fallback CPU) e openvino.
          Get-ChildItem "$pub/runtimes" -Directory -ErrorAction SilentlyContinue |
            Where-Object { $_.Name -match '^(linux|macos|win-arm64|win-x86|coreml)' } |
            Remove-Item -Recurse -Force
          # 4. Peso morto: símbolos de debug, docs XML e o backend Metal (macOS-only) do whisper.
          Get-ChildItem $pub -Recurse -Include *.pdb,*.xml,ggml-metal.metal -File |
            Remove-Item -Force
          $sum = (Get-ChildItem $pub -Recurse -File | Measure-Object Length -Sum).Sum
          Write-Host "Tamanho de publish apos cleanup: $([math]::Round($sum/1MB, 1)) MB"

      - name: Assinar Verso.App.exe (Azure Trusted Signing)
        # Ativa automaticamente quando AZURE_SIGNING_ACCOUNT estiver definido (repo Variables).
        # Enquanto não configurado, o build sai sem assinar (alerta do SmartScreen permanece).
        if: ${{ vars.AZURE_SIGNING_ACCOUNT != '' }}
        uses: azure/artifact-signing-action@v2
        with:
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
          endpoint: ${{ vars.AZURE_SIGNING_ENDPOINT }}
          signing-account-name: ${{ vars.AZURE_SIGNING_ACCOUNT }}
          certificate-profile-name: ${{ vars.AZURE_CERTIFICATE_PROFILE }}
          files-folder: publish/${{ matrix.variant }}
          files-folder-filter: exe
          files-folder-recurse: false
          file-digest: SHA256
          timestamp-rfc3161: http://timestamp.acs.microsoft.com
          timestamp-digest: SHA256
      - name: Empacotar zip
        shell: pwsh
        run: |
          $ver = '${{ steps.version.outputs.version }}'
          $variant = '${{ matrix.variant }}'
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          $zip = "Verso-${ver}-${variant}-win-x64.zip"
          Compress-Archive -Path "publish/$variant/*" -DestinationPath "dist/$zip" -Force
          Write-Host "Criado dist/$zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Verso-${{ matrix.variant }}-win-x64
          path: dist/*.zip
          if-no-files-found: error
          retention-days: 30

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determinar versão/tag
        id: rel
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            # Push de tag: extrair direto
            tag="${GITHUB_REF#refs/tags/}"
            ver="${tag#v}"
            echo "new_tag=false" >> "$GITHUB_OUTPUT"
          else
            # Push para main: auto-incrementar patch da última tag
            latest=$(git tag --list 'v*.*.*' --sort=-v:refname | head -1)
            if [ -z "$latest" ]; then
              ver="0.1.0"
            else
              lastVer="${latest#v}"
              IFS='.' read -r -a parts <<< "$lastVer"
              ver="${parts[0]}.${parts[1]}.$((parts[2] + 1))"
            fi
            tag="v${ver}"
            echo "new_tag=true" >> "$GITHUB_OUTPUT"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "version=$ver" >> "$GITHUB_OUTPUT"

      - name: Criar tag (push para main)
        if: steps.rel.outputs.new_tag == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "${{ steps.rel.outputs.tag }}"
          git push origin "${{ steps.rel.outputs.tag }}"

      - name: Baixar artifacts
        uses: actions/download-artifact@v4
        with:
          path: assets

      - name: Criar GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${{ steps.rel.outputs.tag }}"
          ver="${{ steps.rel.outputs.version }}"
          gh release create "$tag" -R "${{ github.repository }}" \
            assets/Verso-cpu-win-x64/*.zip assets/Verso-gpu-win-x64/*.zip \
            --title "Verso $ver" \
            --notes "Verso $ver (Windows x64)

          - **Verso-${ver}-cpu-win-x64.zip** (~200 MB) — sem GPU, roda em qualquer máquina. Recomendado para quem não tem NVIDIA.
          - **Verso-${ver}-gpu-win-x64.zip** (~960 MB) — inclui runtimes CUDA/Vulkan para aceleração por GPU.

          Build self-contained single-file — não requer instalação do .NET runtime. A pasta raiz contém só o Verso.App.exe (e os nativos de WPF/WebView2); as DLLs gerenciadas estão embutidas no exe.

          App portátil: modelos, áudios, banco de dados e logs ficam numa pasta data/ ao lado do exe — mover a pasta leva tudo junto, sem depender de %AppData%.

          Pré-requisito: Windows 10/11 com o Microsoft Edge WebView2 Runtime (presente por padrão no Windows 11).

          Descompacte e execute Verso.App.exe."